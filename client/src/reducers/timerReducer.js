const initialState = {
  name: '',
  sets: 0,
  running_sets: 0,
  interval: 0,
  rest: 0,
  running: false,
  running_time: 0
}

const timerReducer = (state = initialState, action) => {
  return state
}

export default timerReducer;